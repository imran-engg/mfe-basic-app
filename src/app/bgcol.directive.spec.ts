import { BgcolDirective } from './bgcol.directive';

describe('BgcolDirective', () => {
  it('should create an instance', () => {
    const directive = new BgcolDirective();
    expect(directive).toBeTruthy();
  });
});
